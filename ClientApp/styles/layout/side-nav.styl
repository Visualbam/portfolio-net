// styles
.side-nav
    height 100vh
    background $dark-purple
    border-left 5px $pink solid

    a
        text-decoration none

    .logo
        animation: fadein 1s;
        width 100px
        margin 0 auto

@keyframes fadein {
    from { opacity: 0; }
    to   { opacity: 1; }
}